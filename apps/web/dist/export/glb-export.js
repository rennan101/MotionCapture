import*as THREE2 from"three";import{GLTFExporter}from"three-stdlib";import*as THREE from"three";function bakeClipToTracks(clip,rest,options={}){const smoothing=options.smoothing??0;const tracks=[];if(!rest){return tracks}for(const[boneName,boneData]of Object.entries(rest.bones)){const times=[];const values=[];clip.frames.forEach((frame,i)=>{times.push(frame.timestamp);const rot=frame.canonical.bones?.[boneName]||[0,0,0,1];values.push(rot[0],rot[1],rot[2],rot[3])});if(times.length>1){const track=new THREE.QuaternionKeyframeTrack("."+boneName+"quaternion",times,values);tracks.push(track)}}return tracks}async function exportClipToGlb(clip,options={}){const rest=options.rest||null;const fileStem=options.fileStem||"motion-forge-clip";const tracks=bakeClipToTracks(clip,rest,options);const scene=new THREE2.Scene;const root=new THREE2.Object3D;scene.add(root);if(rest){for(const[name,boneData]of Object.entries(rest.bones)){const bone=new THREE2.Bone;bone.name=name;bone.position.set(boneData.worldPos[0],boneData.worldPos[1],boneData.worldPos[2]);bone.quaternion.set(boneData.localRot[0],boneData.localRot[1],boneData.localRot[2],boneData.localRot[3]);root.add(bone)}}const exporter=new GLTFExporter;const blob=await new Promise((resolve,reject)=>{exporter.parse(scene,result=>{if(result instanceof Error)reject(result);else{const glb=result;resolve(new Blob([glb],{type:"application/octet-stream"}))}},{binary:true})});const fileName=fileStem+".glb";const bytes=blob.size;const diagnostics={bytes,boneCount:rest?Object.keys(rest.bones).length:0,frameCount:clip.frames.length};return{blob,fileName,diagnostics}}export{exportClipToGlb};
//# sourceMappingURL=glb-export.js.map
